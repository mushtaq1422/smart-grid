# Terminal 1 - Blockchain
ganache-cli

# Terminal 2 - Contract deployment
truffle migrate --reset

# Terminal 3 - Backend API
node src/backend/server.js

# Terminal 4 - Frontend
live-server src/frontend

# Terminal 5 - Simulator (optional, can also start via frontend)
node src/simulators/smartMeter/interface.js